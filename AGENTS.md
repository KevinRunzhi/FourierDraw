# AGENTS.md — FourierDraw

本文件是本工程的硬约束。每次改动前先读一遍。与任何口头指令冲突时，以本文件为准；确有冲突时先提出，不要自行改规则。

涉及文件组织、视觉样式或动画实现时，先读 `docs/FourierDraw_IMPLEMENTATION_REFERENCE.md`。

## 项目是什么

一个离线运行的 iOS 数学教具。用户手绘封闭图形，程序做离散傅里叶变换，用一串旋转的圆重新画出来。交付形式是 App Playground（`.swiftpm`），离线评审。

## 绝对禁止

违反其中任何一条，都会制造一个只在最后打包时才暴露的问题。

- 不修改 `Package.swift`。它由 Swift Playgrounds 生成，任何改动都可能让工程在 Playgrounds 里打不开。需要改时先问。
- 不引入 SPM 依赖、不 `import` SwiftUI / Foundation / CoreGraphics 之外的框架。特别是 UIKit、Metal、Accelerate。
- 不新建 `.metal` 文件、不建第二个 target、不用 asset catalog、不读 Bundle 资源、不改 Info.plist。
- 不用 iOS 17 之后的 API，不用任何 beta API。
- 不用 `localStorage` 之类的持久化。本应用无任何持久化需求。
- 预设图形坐标一律硬编码为 Swift 数组常量。

## 数学事实（写错会导致全盘返工）

`N = 512`。频率映射 `freq = (k < N/2) ? k : k - N`，注意是 `<` 不是 `<=`，因此 **freq ∈ [−256, 255]**。

1. **参与项数必须从实际频率集合计数，不能用 `2M+1`。** M ≤ 255 时是 `2M+1`；**M = 256 时是 512，不是 513**——集合里有 −256 而没有 +256。
2. **M = 1 时参与项是 3 个（−1、0、+1），合成轨迹是椭圆，不是圆。**
3. **正负频率位置对称，但振幅通常不成对。** 顺时针画出的标准圆只有 `k = −1` 非零。任何“成对取用”的假设都会出错。
4. **笔尖位置是解析精确的；屏幕上的历史轨迹是 1024（高档）/ 512（低档）点的折线近似。**
5. 输入端要翻转 y 轴（`y → -y`），使数学坐标系 y 向上。正频率在画面上逆时针转，负频率顺时针。

## 核心规则

- **纳入规则是按频率截断 `|k| ≤ M`**，不是“取振幅最大的前 M 项”。滑块范围 1…256，映射 `M = round(1 + 255·p²)`。
- **圆链按频率排序**：`0, +1, −1, +2, −2, …`。不要按振幅排序。这个顺序保证 M 增加时已有的圆一个都不动，也是轨迹增量缓存成立的前提。
- 轨迹增量递推：`trail_M = trail_{M−1} + c_{+M}·e^(i2πMt) + c_{−M}·e^(−i2πMt)`。**M = 256 时 +256 不存在，只加一项。**

## 工作方式

- 一次只做一个文件。不要“把整个模块写出来”。
- 每次输出**完整文件内容**，不要 diff。
- 每次改动后必须编译，零 error 零 warning 才算完成。
- 数学层的改动必须跑验证并把 `print` 输出贴出来，不要口头声称通过。
- 拿不准的地方先问，不要猜一个 API 出来。

## 验证闸门

数学层未通过以下三项，不得开始写渲染层：

1. 顺时针采样的标准圆，DFT 结果应为 `freq == −1` 的项 `amp ≈ 100`，其余 `< 1e-6`
2. `|freq| ≤ 256` 的项数是 512
3. `TrailCache` 增量递推与全量重算逐点误差 `< 1e-9`，含 M = 256 边界
