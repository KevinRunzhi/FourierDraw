# FourierDraw 产品需求文档

**项目代号**|FourierDraw(中文名:圆之绘)
**版本**|v1.0
**日期**|2026-08-31
**提交 DDL**|2026-09-02 22:00
**主题旗**|教育文化

---

## 一、一句话描述

手绘任意图形,用一串旋转的圆把它重新画出来。

## 二、项目背景与约束

本作品用于 SCC 小组赛,提交后由评委离线评审。以下是硬约束,任何设计决策都不得违反:

| 约束项 | 具体要求 | 对本项目的影响 |
|---|---|---|
| 交付形式 | 必须是 App Playground(`.swiftpm`) | 不建 Xcode 项目;Xcode 仅作为编辑器打开 `.swiftpm` |
| 网络 | 离线评审,不可依赖网络 | 零网络请求,零远程资源 |
| 资源 | 全部资源内置于 ZIP 内 | 无图片、无音频、无字体文件;所有画面实时计算生成 |
| 体积 | ZIP ≤ 25 MB | 预估实际体积 < 500 KB,无压力 |
| 体验时长 | 三分钟内可完整体验 | **无欢迎页、无教程页、无登录**,打开即有动画 |
| 附加文档 | 200 字以内 PDF 文字说明 | 见第十一节 |
| ARKit | 全组至少 2 个作品使用 ARKit | 本作品不使用,该指标由队友的作品承担 |

评分维度:功能完整性 3 分、技术难度 3 分、创意性 2 分、呈现效果 2 分。本文档第九节说明每个维度的对应设计。

## 三、目标与非目标

### 3.1 产品目标

让一个完全不懂傅里叶变换的人,在三分钟内亲眼看懂一件事:

> 任何一条闭合曲线,都可以拆解成若干个不同大小、不同转速的圆的旋转叠加。

这不是通过文字解释达成的,而是通过一个可以拖动的滑块——用户自己动手把图形从"一个圆"一路"长"到完整形状,理解在这个过程中自然发生。

### 3.2 明确的非目标

以下内容本版本不做,写在这里是为了防止开发过程中范围蔓延:

- 不做账号、云同步、分享、导出视频
- 不做音频合成(把谐波映射成声音),概念好但一天时间性价比低
- 不做二维图像的频域滤波(那是另一个项目)
- 不做 FFT 优化,朴素 O(N²) 足够
- 不做 iPad 分屏、横竖屏双套布局;竖屏优先,横屏能用即可
- 不做深浅色双主题,只做深色

### 3.3 目标用户

评委(第一位),以及任何在中学接触过三角函数、但没学过傅里叶变换的人。

## 四、核心概念

用户画的一条闭合曲线,可以看作复平面上的一串采样点 z₀, z₁, …, z_{N-1}。对其做离散傅里叶变换,得到 N 个复系数 c_k:

```
c_k = (1/N) · Σ_{j=0}^{N-1} z_j · e^(-i·2π·k·j/N)
```

每个 c_k 对应一个圆:

- **半径** = |c_k|(模长)
- **初相位** = arg(c_k)(辐角)
- **转速** = k(下标,需映射到 [-N/2, N/2) 区间,允许负值即反向旋转)

把这 N 个圆首尾相连——第一个圆的圆心固定,它的边缘点作为第二个圆的圆心,依此类推——所有圆同时按各自转速旋转,最末端那个点划过的轨迹,恰好还原出原始曲线:

```
z(t) = Σ_k c_k · e^(i·2π·k·t)
```

**只取前 M 个最大的圆**,得到的就是原曲线的 M 阶近似。这正是谐波滑块的数学含义。

## 五、功能需求

### 5.1 优先级定义

- **P0**|不做就不能交付,必须完成
- **P1**|直接影响评分,强烈建议完成
- **P2**|锦上添花,时间不足时第一批砍掉

### 5.2 功能清单

#### F1 · 开屏自动演示(P0)

应用启动后立即播放内置预设图形的本轮动画,无任何点击门槛。

- 默认预设:五角星(单笔闭合的 10 顶点轮廓,用代码直接生成;轮廓清晰,圆链运动感强)
- 屏幕底部常驻一行提示文字:「用手指画一个封闭图形试试」
- 提示文字在用户第一次绘制后永久消失

#### F2 · 手绘输入(P0)

- 使用 `DragGesture(minimumDistance: 0)` 采集触摸点
- 绘制过程中实时显示原始笔迹(高饱和色实线)
- 单指绘制,不支持多指
- 抬手后自动闭合:若首尾点距离 > 10pt,将起点追加到序列末尾
- 抬手后自动触发 F3 计算与 F4 播放
- 采样点少于 10 个时视为误触,不触发计算,静默清除

#### F3 · 傅里叶分解(P0)

抬手后一次性执行,全过程应在 100ms 内完成:

1. 等弧长重采样至 N = 512 点
2. 平移点集使质心位于原点
3. 转换为复数序列,执行 DFT
4. 输出按振幅降序排列的 `[Epicycle]` 数组

**等弧长重采样是必须的**。用户画得快的地方点稀、画得慢的地方点密,直接送入 DFT 等于给曲线加了非均匀的时间参数化,重建结果会变形。

#### F4 · 本轮动画渲染(P0)

- 使用 `TimelineView(.animation)` + `Canvas` 实现,目标 60fps
- 每个圆绘制:低透明度灰色圆环 + 稍亮的半径连杆
- 末端笔尖位置累积成轨迹路径,用高饱和色绘制
- 轨迹随时间渐隐(保留最近一个完整周期)
- 动画周期固定 8 秒,循环播放
- 半径小于 0.5pt 的圆不绘制圆环和连杆,但仍参与位置计算

#### F5 · 谐波数量滑块(P1,教学核心)

**这是整个作品最重要的交互,优先级仅次于 P0 主链路。**

- 滑块范围 1 ~ 512,默认值 512(完整还原)
- 拖动时实时改变参与渲染的圆的数量(取振幅最大的前 M 个)
- 滑块上方显示当前值,格式:「谐波 128 / 512」
- 采用非线性映射,低段更细腻:滑块位置 p ∈ [0,1] 映射为 M = round(1 + 511·p²)。低阶变化的视觉差异远大于高阶,线性映射会让前半段几乎看不出区别
- 状态中保存连续的滑块位置 p,渲染时再计算整数 M,避免整数值反向映射造成拖动跳变
- 拖动过程中不重新计算 DFT,只改渲染数量,保证跟手

#### F6 · 预设图形画廊(P1)

底部一排图标按钮,点击后直接加载对应预设并播放:

| 预设 | 优先级 | 选择理由 |
|---|---|---|
| 五角星 | P1 | 默认展示项,尖角多,圆链运动漂亮 |
| 心形 | P1 | 可用参数方程生成,曲线光滑,与星形对比明显 |
| 正方形 | P1 | **教学关键项**,直角处的振铃即吉布斯现象 |
| 汉字「数」 | P2 | 仅作为时间充足时的加分项,展示复杂曲线的还原能力 |

所有预设均由 Swift 代码内置生成或声明,不使用 JSON、不读取 Bundle 资源。Swift Playgrounds 的资源路径行为不稳定,不值得为此排查。

首版只验收五角星、心形、正方形三个预设。汉字「数」不进入首版验收;三个预设完成后仍有余量再考虑实现,不预先制作替代项。

#### F7 · 频谱条形图(P1)

屏幕底部区域,横轴为频率、纵轴为 |c_k| 的条形图。

- 只显示振幅最大的前 64 个谐波(全画 512 条会糊成一片)
- 与 F5 滑块联动:当前启用的谐波高亮,未启用的降为暗灰
- 静态渲染,不做动画

#### F8 · 吉布斯现象提示(P2)

当用户选择「正方形」预设、且谐波数 < 64 时,在画面角落淡入一行小字:

> 直角处的抖动消不掉——这是吉布斯现象

这一句话能显著提升教育属性的说服力,实现成本极低。

#### F9 · 播放控制(P2)

暂停/继续按钮、速度调节(0.5× / 1× / 2×)。有则更好,无则不影响。

## 六、交互流程

### 6.1 三分钟体验脚本

评审是限时的,体验节奏本身需要设计:

| 时间 | 用户所见 |
|---|---|
| 0:00–0:15 | 打开即见一组圆在旋转,正在画出五角星。底部提示「用手指画一个封闭图形试试」 |
| 0:15–0:45 | 用户画一笔,抬手瞬间笔迹淡出,数百个圆从中心展开、串成链条、开始重画他刚画的东西 —— **本作品的高光时刻,必须干净利落** |
| 0:45–1:30 | 用户发现谐波滑块,从 1 拖到 512,图形从一个椭圆逐渐"长"成完整形状 —— **教学价值所在** |
| 1:30–2:15 | 注意到底部频谱条形图,与滑块联动高亮 |
| 2:15–3:00 | 点击预设画廊,尤其是正方形,观察直角处的振铃 |

### 6.2 状态机

```
idle(播放预设)
  ├─ 手指按下 ──→ drawing(显示实时笔迹,暂停本轮动画)
  │                   └─ 抬手 ──→ computing(< 100ms)──→ playing
  ├─ 点击预设 ──→ computing ──→ playing
  └─ 拖动滑块 ──→ 保持当前状态,仅改渲染数量
```

`computing` 状态耗时极短,不需要 loading 指示器。

## 七、视觉设计

深色背景,信息层次靠明度和饱和度区分,不使用装饰性元素。

| 元素 | 规格 |
|---|---|
| 背景 | 近黑(#0A0A0F) |
| 圆环 | 白色,透明度 0.12,线宽 0.5 |
| 连杆 | 白色,透明度 0.35,线宽 1.0 |
| 重建轨迹 | 青色(#00E5FF),线宽 2.5,末端最亮,向前渐隐至透明 |
| 用户实时笔迹 | 橙色(#FF9500),线宽 3.0 |
| 频谱条 | 启用段青色,未启用段白色透明度 0.15 |
| 文字 | 系统字体,白色透明度 0.6,不使用自定义字体 |

布局(竖屏):画布区占上方约 65%,控制区(滑块 + 预设按钮 + 频谱图)占下方约 35%。

## 八、技术方案

### 8.1 技术栈

只用 `SwiftUI` 和 `Foundation` / `CoreGraphics`。**禁止**:UIKit、Metal、`.metal` 文件、SPM 外部依赖、asset catalog、Bundle 资源读取、Info.plist 自定义项、第二个 target。

目标 iOS 17,不使用任何 beta API。

违反上述任一条,都可能制造一个只在最后打包时才暴露的问题。

### 8.2 文件结构

```
FourierDraw.swiftpm/
├── Package.swift              // iOSApplication product(由 Playgrounds 生成,不手写)
├── FourierDrawApp.swift       // @main
├── Math/
│   ├── Complex.swift          // 复数结构体
│   ├── Resample.swift         // 等弧长重采样 + 质心归零
│   └── DFT.swift              // 朴素 DFT,输出 [Epicycle]
├── Views/
│   ├── ContentView.swift      // 布局与状态管理
│   ├── EpicycleCanvas.swift   // TimelineView + Canvas 渲染
│   ├── DrawingLayer.swift     // DragGesture 采集与实时笔迹
│   ├── HarmonicSlider.swift
│   ├── SpectrumBar.swift
│   └── PresetGallery.swift
└── Presets.swift              // 预设图形坐标常量
```

### 8.3 核心数据结构

```swift
struct Complex {
    var re: Double
    var im: Double
    var magnitude: Double { sqrt(re*re + im*im) }
    var phase: Double { atan2(im, re) }
}

struct Epicycle {
    let freq: Int       // 转速,可正可负
    let amp: Double     // 圆半径 = |c_k|
    let phase: Double   // 初相位 = arg(c_k)
}
```

### 8.4 关键实现要点

**负频率映射**|k > N/2 的项必须映射为 `k - N`。否则所有圆同向旋转,画面失真。这是最容易写错的一行,必须验证。

**按振幅排序**|DFT 输出按 `amp` 降序排列。数学上顺序无关,但视觉上大圆套小圆更清晰,且谐波滑块「取前 M 个」的语义依赖此排序。

**渲染上限**|谐波数量 M 与圆环绘制数量必须分开:前 M 个谐波全部参与末端位置计算,保证重建精度;只为前 `min(M, 120)` 个绘制圆环和连杆。Playgrounds 运行在 Debug 模式,512 次 `Path` 描边可能掉帧。120 是初始性能预算,若验证仍掉帧可直接降至 60,不改变重建逻辑。

**动画时间**|使用 `TimelineView` 提供的 `timeline.date` 计算相位,不自行维护帧计数器,避免掉帧导致动画速度漂移。

### 8.5 性能预算

| 项目 | 预算 | 说明 |
|---|---|---|
| DFT 计算(N=512) | < 100ms | 26 万次复数乘法,一次性开销 |
| 单帧渲染 | < 16ms | 最多计算 512 个谐波位置,绘制 120 组圆环和连杆 |
| 冷启动到首帧 | < 1s | 预设由代码内置,无资源解析开销 |
| ZIP 体积 | < 500 KB | 纯代码 |

## 九、评分维度对应

| 维度 | 分值 | 对应设计 |
|---|---|---|
| 功能完整性 | 3 | 手绘 → 分解 → 重建的完整闭环,加预设画廊、谐波滑块、频谱视图,功能自洽无断点 |
| 技术难度 | 3 | 实时 DFT、等弧长重采样、负频率映射、按振幅排序渲染、60fps Canvas 绘制。这些必须在 200 字说明里点出来,否则评委看不到 |
| 创意性 | 2 | 把抽象数学变成可以用手指参与的交互;谐波滑块让"逼近过程"可见,而不是只给结果 |
| 呈现效果 | 2 | 深色背景 + 低透明度圆环 + 高饱和轨迹,视觉聚焦明确;打开即有动画,零门槛 |

## 十、开发排期(单日)

前五小时必须产出可交付版本,其后全部是加分项。

| 时段 | 任务 | 完成标志 |
|---|---|---|
| H1 | Swift Playgrounds 新建 App → Xcode 打开 `.swiftpm` → 模拟器跑通空白页 → **回 Playgrounds 再打开一次确认** | 双向可打开 |
| H2–H3 | `Complex` / `Resample` / `DFT` 三个文件 + 单元验证 | 圆测试通过(见 12.1) |
| H4–H5 | Canvas 静态圆链 → 接 TimelineView 转起来 → 接手绘输入 | **主链路闭环,此时已可交付** |
| H6–H7 | 谐波滑块 + 预设画廊 | F5、F6 完成 |
| H8 | 频谱条形图 + 视觉打磨 | F7 完成 |
| H9 | 真机完整走一遍三分钟脚本 → 写 200 字 PDF → 打包 ZIP | 提交完成 |

**硬性纪律**:DDL 前 3 小时停止写新功能,只做验证和打包。

## 十一、200 字说明初稿

> FourierDraw 是一个离线运行的数学可视化教具。
>
> 用手指在屏幕上画任意封闭图形,应用将轨迹等弧长重采样为 512 点复数序列,实时执行离散傅里叶变换,得到一组旋转本轮——每个圆的半径为傅里叶系数的模长,转速为其频率(含负频率)。这些圆首尾相接同步旋转,末端笔尖划过的轨迹精确还原原图。
>
> 核心交互是谐波数量滑块:从 1 个圆拖到 512 个圆,可以亲眼看到形状如何从最粗的近似逐步"生长"成完整图形。选择正方形预设并降低谐波数,直角处会出现永远无法消除的振铃——这就是吉布斯现象。
>
> 全部画面由数学实时生成,零素材、零网络、零第三方依赖,仅使用 SwiftUI 与 Foundation。

(约 260 字,提交前需压缩至 200 字以内,优先删减第一段和最后一段。)

## 十二、验收标准

### 12.1 数学层验证(必须最先通过)

构造半径 100、圆心在原点的标准圆的 512 个等角采样点,输入 `dft`:

- 预期:仅 `freq == 1` 的项 `amp ≈ 100`,其余所有项 `amp < 1e-6`
- 若不通过,检查频率映射(`k > N/2` 是否减 N)与指数符号

`resample` 验证:输入 (0,0) 到 (100,0) 的两点直线,重采样至 5 点,结果应为 x = 0, 20, 40, 60, 80。

**数学层不过,后续所有视觉问题都会指向错误方向,必须先验。**

### 12.2 交付验收

- [ ] 在 Mac 版 Swift Playgrounds 中可直接打开并运行
- [ ] 在 iPad/iPhone 真机上完整走通三分钟脚本
- [ ] 全程飞行模式运行正常
- [ ] 冷启动 1 秒内出现动画
- [ ] 手绘任意图形,抬手后 1 秒内开始重建
- [ ] 谐波滑块拖动跟手,无卡顿
- [ ] 五角星、心形、正方形三个 P1 预设全部可正常加载
- [ ] ZIP 体积 < 25 MB
- [ ] PDF 说明 ≤ 200 字

## 十三、风险与预案

| 风险 | 概率 | 预案 |
|---|---|---|
| DFT 频率映射写错,重建图形变形 | 中 | 12.1 的圆测试作为第一道闸门,H3 结束前必须通过 |
| Playgrounds 中掉帧 | 中 | 渲染圆数上限降至 60;必要时 N 从 512 降到 256 |
| Codex 生成的 SwiftUI API 不存在 | 中 | 全程挂 Xcode 编译反馈,不接受未编译通过的代码 |
| 汉字预设坐标提取耗时超预期 | 高 | 已降为 P2,不进入首版验收;无余量时直接不做 |
| Xcode 能跑但 Playgrounds 打不开 | 低 | H1 已做双向验证;全程不碰 8.1 的禁止项 |
| 时间不够 | 中 | 按 P2 → P1 → P0 逆序砍功能,主链路(F1–F4)在 H5 已完成 |

## 十四、待确认事项

1. **「教育文化」旗的归属**需要跟队友确认,避免撞车。这件事比技术选型更紧急。
2. 组内其余作品是否已覆盖 ARKit 的两个名额。
